# Before pushing, I would verify:

whoami

# If it returns:

sabderra

# then /home/sabderra/data/... is the correct path for evaluation.

One more thing: after changing the paths, remove the old volumes and test from scratch:

# docker-compose -f ./srcs/Docker-compose.yml down -v
# sudo rm -rf /home/sabderra/data/mariadb/*
# sudo rm -rf /home/sabderra/data/wordpress/*
# docker-compose -f ./srcs/Docker-compose.yml up --build